[config]
# Select one option for each param:
# For 'signal_detection': tf, fw, bert, tf+fw, bert+fw. Default: bert+fw, Fastest: tf
# For 'param_detection': PD, TE, SA. Default: TE, Fastest: PD
# method: Pipeline, Decomposition, Signal, Parameter

signal_detection = tf
param_detection = PD
method = Pipeline